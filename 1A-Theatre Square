    #include <iostream>
    using namespace std;
    int main(int argc, char** argv) {
    long long a,b,c;
    cin>>a >> b>> c;
    long long x,y;
    x =a/c ;
    y = b/c;
    if(a%c!=0)
    ++x;
    if(b%c!=0)
    ++y;
    cout<<x*y;
    	return 0;
    }
