#include <iostream>
#include <bits/stdc++.h>
using namespace std;
int main(int argc, char** argv) {
int n, count=0;
string s;
cin>>n>>s;
for(int i =0;i<n;i++){
	if(s[i]==s[i+1])
	count++;
	
}
cout<<count<<endl;
	return 0;
}
/*code optimization
scanf("%d",&n);
scanf("%s",s);
char now =stone[0];
for(int i =0;i<n;i++){
if(s[i]==now)
count++;
else
//to shift the element to the second element 
now=s[i];
}
printf("%d\n",count);
